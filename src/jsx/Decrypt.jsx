import React from "react";
import "../css/Decrypt.css";

export default class Decrypt extends React.Component {
  state = {};

  render() {
    return (
      <div className="decrypt">
        <p style={{paddingTop: 200}}>Decrypt</p>
      </div>
    );
  }
};